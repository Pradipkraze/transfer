import rasterio
from rasterio.warp import calculate_default_transform, reproject, Resampling
from rasterio.enums import Resampling as ResamplingEnum
import numpy as np

def resample_raster(src_path, dst_path, target_pixel_size):
    with rasterio.open(src_path) as src:
        transform, width, height = calculate_default_transform(
            src.crs, src.crs, src.width, src.height, *src.bounds, resolution=target_pixel_size
        )
        profile = src.profile.copy()
        profile.update({
            'transform': transform,
            'width': width,
            'height': height,
            'dtype': 'int16'
        })

        with rasterio.open(dst_path, 'w', **profile) as dst:
            for i in range(1, src.count + 1):
                reproject(
                    source=rasterio.band(src, i),
                    destination=rasterio.band(dst, i),
                    src_transform=src.transform,
                    src_crs=src.crs,
                    dst_transform=transform,
                    dst_crs=src.crs,
                    resampling=ResamplingEnum.bilinear
                )

def adjust_to_extent(src_path, target_extent, dst_path):
    with rasterio.open(src_path) as src:
        profile = src.profile.copy()
        
        transform = rasterio.transform.from_bounds(
            *target_extent, src.width, src.height
        )

        profile.update({
            'transform':  extent-mask,,htandsigths nigh etc dimensws
